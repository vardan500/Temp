{
  "properties": {
    "displayName": "OpsExcellence - Restrict allowed values for 'backup' tag",
    "policyType": "Custom",
    "mode": "Indexed",
    "description": "Audits or denies resources whose 'backup' tag value is not in the approved list, standardizing backup-frequency intent. Only evaluates when the tag exists.",
    "metadata": { "category": "Tags", "pillar": "OperationalExcellence", "version": "1.0.0" },
    "parameters": {
      "effect": {
        "type": "String",
        "metadata": { "displayName": "Effect" },
        "allowedValues": [ "Audit", "Deny", "Disabled" ],
        "defaultValue": "Audit"
      },
      "tagName": {
        "type": "String",
        "metadata": { "displayName": "Tag Name" },
        "defaultValue": "backup"
      },
      "allowedValues": {
        "type": "Array",
        "metadata": { "displayName": "Allowed backup values" },
        "defaultValue": [ "daily", "weekly", "monthly", "none" ]
      }
    },
    "policyRule": {
      "if": {
        "allOf": [
          {
            "field": "[concat('tags[', parameters('tagName'), ']')]",
            "exists": "true"
          },
          {
            "not": {
              "field": "[concat('tags[', parameters('tagName'), ']')]",
              "in": "[parameters('allowedValues')]"
            }
          }
        ]
      },
      "then": { "effect": "[parameters('effect')]" }
    }
  }
}
